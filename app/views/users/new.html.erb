<h1>Register</h1>
<%= form_for(@user) do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
  <%= render 'form', :f => f %>
  <div class="actions">
    <%= f.submit "Register" %>
  </div>
<% end %>